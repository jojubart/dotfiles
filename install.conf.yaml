- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    # neovim stuff
    ~/.config/nvim: nvim

- shell:
   - [git submodule update --init --recursive, Installing submodules]
